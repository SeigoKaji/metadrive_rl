# MetaDrive is cloned separately as ../metadrive and installed from source.
# Update that checkout before installing when the latest main branch is wanted.
-e ../metadrive

# Exact versions resolved in the verified Python 3.12 environment are recorded
# in requirements.lock.txt. Keep this file limited to direct dependencies.
stable-baselines3[extra]
Pillow
opencv-python
openpyxl
pytest
